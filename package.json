{
  "name": "lichess-pgn-viewer",
  "license": "GPL-3.0-or-later",
  "version": "0.0.1",
  "keywords": [
    "chess",
    "lichess",
    "game",
    "pgn",
    "viewer",
    "typescript"
  ],
  "type": "module",
  "module": "dist/main.js",
  "types": "dist/main.d.ts",
  "dependencies": {
    "@bity/oauth2-auth-code-pkce": "^2",
    "chessground": "^8",
    "chessops": "^0.12",
    "snabbdom": "^3"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^21",
    "@rollup/plugin-node-resolve": "^13",
    "@rollup/plugin-typescript": "^8",
    "http-server": "^14",
    "prettier": "^2",
    "rollup": "^2",
    "rollup-plugin-scss": "^3",
    "rollup-plugin-terser": "^7",
    "sass": "^1",
    "tslib": "^2",
    "typescript": "^4"
  },
  "scripts": {
    "compile": "tsc --incremental --declaration --inlineSourceMap",
    "dev": "$npm_execpath run compile && rollup --config --watch",
    "watch": "$npm_execpath run compile && rollup --config",
    "prod": "$npm_execpath run compile && rollup --config --config-prod",
    "demo": "http-server demo"
  }
}
